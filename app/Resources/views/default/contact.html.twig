{% extends 'base.html.twig' %}

{% block body %}
    <p>Contact</p>
    {{ form_start(form) }}
    {{ form_rest(form) }}
    <input type="submit" value="send" />
    {{ form_end(form) }}
{% endblock %}